{
  "name": "sbx-kit-pi",
  "version": "0.1.1",
  "private": true,
  "description": "Validation tests for the Pi OpenAI Codex Docker Sandbox kit",
  "scripts": {
    "audit": "npm audit --audit-level=moderate",
    "test": "node --test"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.4",
    "typebox": "1.3.7",
    "yaml": "2.9.0"
  },
  "engines": {
    "node": ">=20"
  }
}
